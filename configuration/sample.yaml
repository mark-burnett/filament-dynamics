name: 'Sample session name'


parameter_generator:
    simulation:
        class_name: bind_class
        parameters:
            argument_name: parameter_name
        fixed_parameters:
            argument_name: literal

    analysis:
        class_name: bind_class
        parameters:
            argument_name: parameter_name
        fixed_parameters:
            argument_name: literal

    objective:
        class_name: bind_class
        parameters:
            argument_name: parameter_name
        fixed_parameters:
            argument_name: literal


parameter_constraints:
    sample_session_parameter:
        type:  fixed
        value: 11.6


model:
    transitions:
        bind_label:
            class_name: bind_class
            parameters:
                argument_name: parameter_name
            fixed_parameters:
                argument_name: literal


experiments:
    experiment_a:
        parameter_constraints:
            sample_experimental_parameter:
                type:  fixed
                value: 72.1

        simulation:
            parameter_constraints:
                sample_simulation_parameter:
                    type:  fixed
                    value: 72.1

            strategy_element_a:
                bind_label:
                    class_name: bind_class
                    parameters:
                        argument_name: parameter_name
                    fixed_parameters:
                        argument_name: literal


        analyses:
            analysis_a:
                # XXX should there be parameter constraints?
                parameter_constraints:
                    sample_analysis_parameter:
                        type:  fixed
                        value: 11.6

                strategy:
                    - bind_label:
                        class_name: bind_class
                        parameters:
                            argument_name: parameter_name
                        fixed_parameters:
                            argument_name: literal


        objectives:
            objective_a:
                parameter_constraints:
                    sample_objective_parameter:
                        type:  fixed
                        value: 11.6

                strategy_element:
                    bind_label:
                        class_name: bind_class
                        parameters:
                            argument_name: parameter_name
                        fixed_parameters:
                            argument_name: literal
