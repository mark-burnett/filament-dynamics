name: 'Sample session name'


controllers:
    simulation:
        class_name: bind_class
        variable_arguments:
            argument_name: parameter_name
        fixed_arguments:
            argument_name: literal

    analysis:
        class_name: bind_class
        variable_arguments:
            argument_name: parameter_name
        fixed_arguments:
            argument_name: literal

    objective:
        class_name: bind_class
        variable_arguments:
            argument_name: parameter_name
        fixed_arguments:
            argument_name: literal


parameters:
    sample_session_parameter: 11.6


model:
    transitions:
        bind_label:
            class_name: bind_class
            variable_arguments:
                argument_name: parameter_name
            fixed_arguments:
                argument_name: literal


experiments:
    experiment_a:
        parameters:
            sample_experimental_parameter: 72.1

        simulation:
            strategy_element_a:
                bind_label:
                    class_name: bind_class
                    variable_arguments:
                        argument_name: parameter_name
                    fixed_arguments:
                        argument_name: literal


        analyses:
            analysis_a:
                # XXX should there be analysis parameters?
                parameters:
                    sample_analysis_parameter: 11.6

                strategy:
                    - class_name: bind_class
                      variable_arguments:
                          argument_name: parameter_name
                      fixed_arguments:
                          argument_name: literal


        objectives:
            objective_a:
                parameters:
                    sample_objective_parameter: 11.6

                strategy:
                    - bind_label:
                        class_name: bind_class
                        variable_arguments:
                            argument_name: parameter_name
                        fixed_arguments:
                            argument_name: literal
