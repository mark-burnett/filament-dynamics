import:
    - 'parameters/tip_release_rate.yaml'

global_parameters:
    actin_concentration: 0.3

    cleavage_rate: 0.3

parameter_specifications:
    stall_force:
        simulation:
            number_of_filaments:
                lower_bound: 1
                upper_bound: 10
                mesh_type: linear
                num_points: 10
