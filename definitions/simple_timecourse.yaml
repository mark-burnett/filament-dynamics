name: Basic Timecourse

import:
    # Experiment
    - 'experiments/simple_timecourse.yaml'

    # Parameters
    - 'parameters/melki_conditions.yaml'
    - 'parameters/melki_fnc.yaml'
    - 'parameters/first_paper.yaml'
    - 'parameters/tip_release_rate.yaml'
#    - 'parameters/small_statistics.yaml'
#    - 'parameters/medium_statistics.yaml'

    # Use the random model
    - 'random_cooperative_model.yaml'
    - 'fit_rates/rho_1.yaml'

experiments:
    adp_copoly:
        parameters:
            simulation_duration: 1000.1
            sample_period: 1

            number_of_simulations: 20
            number_of_filaments: 1

parameter_specifications:
    adp_copoly:
        simulation:
            fraction_adp:
                lower_bound: 0
                upper_bound: 0
                mesh_type: linear
                num_points: 1
