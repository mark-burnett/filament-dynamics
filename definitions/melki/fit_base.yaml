import:
    # Experiment
    - 'experiments/melki_fit.yaml'

    # Parameters
    - 'parameters/melki_conditions.yaml'
    - 'parameters/first_paper.yaml'
    - 'parameters/tip_release_rate.yaml'


global_parameters:
    number_of_filaments: 1000

    simulation_duration: 2500.001

    sample_period: 1
