name: 'SF Depoly - stats, Fast barbed rate (random)'

import:
    - 'sf_depoly/base.yaml'
    - 'random_cooperative_model.yaml'
    - 'fit_rates/rho_1.yaml'

global_parameters:
    barbed_tip_release_rate: 7.5

    number_of_filaments: 10

parameter_specifications:
    simple_depoly:
        simulation:
            dummy_parameter:
                lower_bound: 1
                upper_bound: 1
                mesh_type: linear
                num_points: 1
