name: Pi Association

import:
    # Experiment
    - 'experiments/tagged_pi_poly.yaml'

    # Model
    - 'components/barbed_polymerization.yaml'
    - 'components/pointed_polymerization.yaml'
    - 'components/tagged_barbed_polymerization.yaml'
    - 'components/tagged_pointed_polymerization.yaml'

    - 'components/pi_release_in_solution.yaml'
    - 'components/pi_association.yaml'
    - 'components/tagged_pi_release_in_solution.yaml'
    - 'components/tagged_pi_association.yaml'

    - 'components/melki_cooperative_release.yaml'
    - 'components/tagged_melki_cooperative_release.yaml'

    # Parameters
    - 'parameters/fujiwara_reverse_release.yaml'
    - 'parameters/in_solution_release_rate.yaml'
    - 'parameters/polymerization_fujiwara_full.yaml'
    - 'parameters/melki_fit.yaml'
    - 'parameters/melki_conditions.yaml'
    - 'parameters/first_paper.yaml'

experiments:
    pi_poly:
        parameters:
            simulation_duration: 15000

            sample_period: 10


parameter_specifications:
    pi_poly:
        simulation:
            initial_pi_concentration:
                lower_bound: 10000
                upper_bound: 1000000
                mesh_type: log
                num_points: 21

            release_cooperativity:
                lower_bound: 1
                upper_bound: 1000000
                mesh_type: log
                num_points: 21
