name: Jegou Depolymerization Timecourse, rho = 100000

import:
    # Experiment
    - 'experiments/simple_depoly.yaml'

    - 'random_cooperative_model_depoly.yaml'

    # Parameters
    - 'parameters/jegou_2011_conditions.yaml'
    - 'parameters/first_paper.yaml'

    - 'fit_rates/rho_100000.yaml'

experiments:
    simple_depoly:
        parameters:
            simulation_duration: 1600.1
            polymerization_duration: 600
            sample_period: 1

            number_of_simulations: 1
            number_of_filaments: 1

parameter_specifications:
    simple_depoly:
        simulation:
            dummy_parameter:
                lower_bound: 1
                upper_bound: 100
                mesh_type: linear
                num_points: 100
